P1.1/On a crée une classe vecteur comme demandé comprenant un tableau dynamique de double (vector<double>) comme attribut en privée et les méthodes de la consigne en public. On a également rajouté une méthode getcood(size_t indice) qui est un accesseur sur les coordonnées du vecteur; et une méthode dim(const Vecteur &v) vérifiant si la dimension du vecteur passé en argument est la même que l'attribut vect de l'instance courante. En effet elle est utile dans la definition des autres méthodes mais pas pour le programmeur utilisateur
 
P1.2/On a choisis de prendre la dimension du premier des deux opérandes, on a trouvé que c'etait le plus
naturel dans le code. Bien que l'on perd des infos du deuxieme vecteur, on pourra choisr lequel des deux 
mettre en premier ce qui donnera d'autres possibilités au code.

P4.1/Non on n'a pas ajouté de constructeur de copie, par ce que sa version par défaut est déjà présente et qu'une copie de surface est suffisante.

P4.2/
a)Il faudrait rajouter ces éléments comme attributs par exemple(teta, phi et r)
b)Etant donné qu'il existe déjà un constructeur prenant trois arguments double il faudrait que les types 
du nouveau soient différents ce qui parait peu pratique pour des angles et une longueure. 

P4.3/On ajouté ces opérateurs:
En interne: +=, -=, ^=(n'a pas trop de sens mais est pratique pour mettre le produit vectoriel en externe), *= ( la multiplication d'un vecteur par un scalaire), ~(qui prend la direction et le sens du vecteur, remplace unitaire), *, - (prend l'opposé) et == .Ces quatres derniers ont été mis en interne pour pouvoir accéder a l'attribut sans avoir à utiliser de methode get, bien qu'un nouveau vecteur est crée pour certains, on a fait ce choix pour simplifier le code.
En externe: +, -, *(la multiplication par un scalaire) et <<.

P6.1/On a conçu integrateur comme callse abstraite donc sans attributs et avec une methode virtuelle pure integre() que l'on pourra substituer pour specifier la méthode d'integration. On a egalement ajouté un destructeur virtuelle. 

P6.2/IntegrateurEulerCromer est un un Integrateur en effet c'est une sous classe de la classe Integrateur et la methode integre de cette sous classe override l'original.

P7.1/ Nous avons créé une sous-classe de ChampForces, ForceUniforme qui contient comme attribut un Vecteur intensite qui représente l'intensité de champ de force uniforme. Dans cette classe, nous avons aussi redéfini la méthode force qui retourne la masse de l'ObjetPhysique en argument multipliée par l'intensité du champ de force. Nous avons aussi ajouté un constructeur et surchargé l'opérateur d'affichage << pour cette classe. La classe GravitationConstante, est, elle une sous classe de ForceUniforme; nous avons crée un constructeur par defaut pour cette classe qui initialise intensite à la valeur du Vecteur g défini la semaine dernière dans le fichier constantes.h.

P7.2/ Nous avons défini la classe PointMateriel comme une sous-classe de ObjetPhysique (ObjetPhysique étant une sous-classe d'ObjetMobile). Le constructeur d'un PointMateriel est similaire à celui d'un ObjetPhysique (général). Il prend en paramètre les Vecteur e et e_prime, une référence sur un objet de type Contrainte et une sur un objet de type ChampForces, et enfin une masse, charge et dimension (de l'espace physique d'évolution de l'objet) tous de type double; il appelle le constructeur d'ObjetPhysique sur ces arguments. Nous avons aussi défini une méthode E_cin qui retourne l'énergie cinétique du PointMatériel.

P8.1/Dessinne_sur est une méthode virtuelle pure. En effet on utilisera des pointeurs ou des references pour redéfinir cette méthode dans des sous classes de Dessinable. Ce qui constitue le polymorphisme.

P8.2/Il faut faire attention aux copies de classes définies par des pointeurs. En effet il faut des copies profondes et pas des copies de surfaces afin de copier la reférence et l'objet sur lequel pointe le pointeur. Afin de ne pas modifier l'objet en meme temps que la reférences copiées.

P8.3/attributs private: -tableau dynamique de unique_ptr d'objetphysique
                        -tableaux dynamiques de shared_ptr de Contrainte, de                                ChampForces
                        -le temps
                        -un unique_ptr sur un Integrateur
Les pointeurs sont nécessaires afin de pratiquer le polymorphisme. Les contraintes et les champforces sont partagés entre les objets d'ou les shared ptr. On a utilisé des unique_ptr pour faciliter la gestion de memoire 
  On a juste rajouté un constructeur prenant tous les attributs mentionnés plus tot en plus d'un constructeur par défaut.
  Des manipulateurs comme dans la consigne et une methode affiche créeant la sortie pour la surcharge de l'opérateur <<. 
